<help page="Events Log">

<p>В &#8216;Журнале&#8217; объекта отражена вся история связанных с данным объектом событий (таких как создание, сохранение или удаление).</p>

<p>Таблица событий содержит следующие колонки:</p>

<dl>
 <dt>Имя (Name)</dt>
 <dd>Тип происшедшего события.</dd>

 <dt>Кем вызвано (Triggered By)</dt>
 <dd>Имя пользователя, вызвавшего событие.</dd>

 <dt>Время (Timestamp)</dt>
 <dd>Дата и время события.</dd>

 <dt>Атрибуты (Attributes)</dt>
 <dd>Вся прочая информация, которая сопутствует событию. Например, если событие соответствует перемещению материалов на лругой стол, то в данной колонке указывается имя этого стола.</dd>
</dl>

<p>По умолчанию события отображаются в порядке возникновения, начиная со наиболее ранних, Нажмите на заголовок любой колонки в таблице для того, чтобы изменить порядок сортировки.</p>

</help>
