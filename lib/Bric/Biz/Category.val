-- -----------------------------------------------------------------------------
-- Catergory.val
--
-- VERSION: $Revision: 1.4 $
--
-- Test values.
--

DELETE FROM category        WHERE id IN (0);
DELETE FROM grp             WHERE id IN (700);

-- -----------------------------------------------------------------------------
-- Base Group

INSERT INTO category (id,directory, uri, parent_id, name, description) 
VALUES (0, '', '/', 0, 'Root Category', 'Root Category');
