-- -----------------------------------------------------------------------------
-- Catergory.val
--
-- VERSION: $Revision: 1.5 $
--
-- Test values.
--

DELETE FROM category        WHERE id IN (0);
DELETE FROM grp             WHERE id IN (700);

-- -----------------------------------------------------------------------------
-- Base Group

INSERT INTO category (id,directory, uri, parent_id, name, description, asset_grp_id) 
VALUES (0, '', '/', 0, 'Root Category', 'Root Category', 68);

